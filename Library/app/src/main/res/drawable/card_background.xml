<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Normal durumda arka plan rengi -->
    <item android:state_enabled="true" android:drawable="@android:color/white"/>

    <!-- Tıklanmış durumda arka plan rengi -->
    <item android:state_pressed="true" android:drawable="@android:color/darker_gray"/>

    <!-- Odaklanmış durumda arka plan rengi -->
    <item android:state_focused="true" android:drawable="@android:color/holo_blue_dark"/>
</selector>
